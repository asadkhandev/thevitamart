{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }

  /*styles from here*/

.row {
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
}

.align-center {
  align-items: center;
}

.col {
  flex: 1;
  padding: 15px;
}


.col-left {
  max-width: 400px;
}

.section-title {
  font-size: 18px;
  font-weight: 700;
  margin-bottom: 20px;
}

.mission-description {
  font-size: 12px;
  font-weight: 400;
  color: #333333;
  margin-bottom: 25px;
  line-height: 1.8;
}

/* 
.btn {
    border: solid 1px #000000;
  display: inline-block;
  text-decoration: none;
  padding: 12px 24px;
  font-size: 12px;
  font-weight: 700;
  border-radius: 4px;
  transition: all 0.3s ease;
}

.btn-primary {
  background-color: #111111fc;
  color: #fff;
}

.btn-primary:hover {
  background-color: #17171775;
}
 */

.col-right {
  text-align: right;
}

.mission-image img {
  max-width: 100%;
  height: auto;
}


@media (max-width: 768px) {
  .row {
    flex-direction: column;
    text-align: center;
  }

  .col-left,
  .col-right {
    max-width: 100%;
  }

  .col-right {
    text-align: center;
    margin-top: 30px;
  }
}


  

  
{%- endstyle -%}

<div class="our_mission-section color-{{ section.settings.color_scheme }} gradient{% unless section.settings.background_style == 'none' and settings.text_boxes_border_thickness > 0 or settings.text_boxes_shadow_opacity > 0 %} background-{{ section.settings.background_style }}{% endunless %}{% if section.settings.title == blank %} no-heading{% endif %}">
  <div
    class="section-{{ section.id }}-padding page-width isolate{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}"
    {% if settings.animations_reveal_on_scroll %}
      data-cascade
    {% endif %}
  >

    {% comment %}
    {% if section.settings.title != blank %}
      <div class="halo-block-header text-center">
        <h3 class="title">
          <span class="text">{{ section.settings.title }}</span>
        </h3>
      </div>
    {% endif %}
    {% endcomment %}

    {% comment %}
    html
    {% endcomment %}
    

<div class="row align-center">
        
     
        <div class="col col-left">
          <div class="mission-content">
            <h2 class="section-title">OUR MISSION – WELLNESS FOR LIFE!</h2>
            <p class="mission-description">
              At Nutrifactor we aim to explore the goodness of nature<br>
              with innovation. We are dedicated to play our role in<br>
              building a happy & healthy community.
            </p>
            <a href="#" class="button">LEARN MORE</a>
          </div>
        </div>

     
        <div class="col col-right">
          <div class="mission-image">
            <img src="https://cdn.shopify.com/s/files/1/0707/8104/2880/files/our-mission-img.svg?v=1753687505" alt="Wellness Illustration" />
          </div>
        </div>

      </div>



    
  </div>
</div>

{% schema %}
{
  "name": "Our Mission",
  "class": "section",
  "tag": "section",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "inline_richtext",
      "id": "title",
      "label": "Heading"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.all.colors.label",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "t:sections.all.padding.section_padding_heading"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_top",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_bottom",
      "default": 36
    }
  ],
  "blocks": [
    {
      "type": "tag",
      "name": "Collection Tag",
      "settings": [
        {
          "type": "collection",
          "id": "collection",
          "label": "Select Collection"
        },
        {
          "type": "checkbox",
          "id": "important",
          "label": "Mark as Important",
          "default": false
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Our Mission",
      "blocks": [
        {
          "type": "tag"
        },
        {
          "type": "tag"
        },
        {
          "type": "tag"
        }
      ]
    }
  ]
}
{% endschema %}
